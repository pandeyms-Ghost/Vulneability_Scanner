In this first we created port scanner class which scans for open port and service banner running on them.
<br>
Then we imported it in our main code file and we cross checked for vulnerable services running on user inputed port range by proving our list of vulnerable services stored on a file.
